---
title: Seguridad
description: CÃ³mo Velo aÃ­sla las apps y en quÃ© podÃ©s confiar.
---

import { Aside } from '@astrojs/starlight/components';

Velo Deploy hereda su modelo de seguridad de systemd. Cada app corre en una unidad endurecida sin acceso al resto del sistema.

## Usuario Linux por app

Cuando se despliega una app, Velo crea un usuario Linux dedicado sin password, sin login shell y sin directorio home:

```
velo-<appname>:x:1001:1001::/nonexistent:/usr/sbin/nologin
```

La app corre bajo este usuario. No puede leer archivos de otros usuarios, y el SO nunca le va a presentar un prompt de login.

## Unidad systemd endurecida

Cada archivo de unidad incluye las siguientes directivas:

| Directiva | Efecto |
| --- | --- |
| `ProtectSystem=full` | `/usr`, `/boot`, `/etc` se montan read-only. |
| `ProtectHome=true` | La app no puede leer `/home`, `/root` ni `/run/user`. |
| `PrivateTmp=true` | La app tiene su propio namespace de `/tmp`. |
| `NoNewPrivileges=true` | La app no puede escalar privilegios con binarios setuid. |
| `ReadWritePaths=` | Los paths writable se limitan al dir de la app y `/tmp`. |
| `PrivateDevices=true` | La app no puede hablar con devices crudos. |
| `ProtectKernelTunables=true` | Se bloquean escrituras a `/proc` y `/sys`. |
| `RestrictNamespaces=true` | La app no puede crear nuevos namespaces. |

`MemoryDenyWriteExecute` **no** se activa. Node.js (V8 JIT) necesita memoria ejecutable; W^X tiraría el proceso.

Un ejemplo completo:

```ini
[Unit]
Description=Velo Deploy app: my-api
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=velo-my-api
Group=velo-my-api
WorkingDirectory=/opt/deploy/apps/my-api
ExecStart=/opt/deploy/node/24/bin/node index.js
Restart=on-failure
RestartSec=5
Environment=NODE_ENV=production
Environment=PORT=3000
Environment=HOST=127.0.0.1
EnvironmentFile=-/etc/velo-deploy/apps/my-api.env

NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=full
ProtectHome=true
ReadWritePaths=/opt/deploy/apps/my-api /tmp
PrivateDevices=true
ProtectKernelTunables=true
RestrictNamespaces=true

[Install]
WantedBy=multi-user.target
```

## Aislamiento de filesystem

La app puede:

- Leer y escribir `/opt/deploy/apps/<app>/`.
- Leer y escribir `/tmp` (namespace privado).
- Leer todo en `/usr`, `/etc`, `/var` (montados read-only).
- Hacer conexiones de red salientes.

La app **no puede**:

- Leer los homes de otros usuarios.
- Escribir fuera de su propio directorio (excepto su propio `/tmp`).
- Spawnear procesos setuid.
- Montar filesystems.
- Hablar con block devices crudos.

## Aislamiento de red

Caddy es el Ãºnico proceso escuchando en un puerto pÃºblico. Las apps Node se bindean a `localhost` Ãºnicamente â€” no son accesibles directo desde la red. Todo el trÃ¡fico fluye por el reverse proxy de Caddy, que termina TLS y forwardea al upstream correcto.

## Secretos

<Aside type="danger" title="No commitees secretos a Git">
	Pon los secretos de runtime en `/etc/velo-deploy/apps/<name>.env`. Velo los cablea a la unidad systemd con `EnvironmentFile=-…`, así que un redeploy no los borra.
</Aside>

```bash
sudo install -m 0640 /dev/null /etc/velo-deploy/apps/my-api.env
sudo tee /etc/velo-deploy/apps/my-api.env >/dev/null <<'EOF'
DATABASE_URL=postgres://...
EOF
sudo systemctl restart velo-my-api
```

Las directivas extra de Caddy que tienen que sobrevivir un redeploy van en `/etc/velo-deploy/caddy/<name>.snippet`.

## Seguridad del webhook

`velo-deploy-watcher` exige `X-Hub-Signature-256` (HMAC-SHA256). El instalador escribe un secreto aleatorio en `/etc/velo-deploy/webhook.secret`. Usá el mismo valor como secret del webhook de GitHub. Sin firma válida, la respuesta es `401`.

También podés usar `VELO_WEBHOOK_SECRET` o `webhook_secret` en `config.json`.

## Supply chain

Las dependencias Go están pineadas en `go.sum`. El pipeline firma `SHA256SUMS` con [cosign](https://github.com/sigstore/cosign) (keyless, OIDC de GitHub) y adjunta `SHA256SUMS.bundle`. El instalador verifica SHA256 siempre, y el bundle de cosign cuando `cosign` está instalado.

## Reportar una vulnerabilidad

MirÃ¡ [SECURITY.md](https://github.com/antojsh/velo-deploy/blob/master/SECURITY.md) en GitHub.
